# dotNET 8.0 is currently needed for nuget-license; See https://github.com/sensslen/nuget-license/pull/110

name: Publish

on:
  release:
    types: 
      - published

jobs:
  build:
    runs-on: ubuntu-latest
    permissions: write-all
    steps:
      - uses: actions/checkout@v3
        with:
          submodules: recursive
      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: |
            8.0.x 
            9.0.x
      - name: Install licensing information tool
        run: dotnet tool install --global nuget-license --version 3.0.14
      - name: Build viewer
        run: dotnet build src/FEntwumS.NetlistViewer/FEntwumS.NetlistViewer.csproj -c Release -o publish
      - name: Generate licensing information for viewer
        id: license
        run: nuget-license -i ./src/FEntwumS.NetlistViewer/FEntwumS.NetlistViewer.csproj -override ./src/FEntwumS.NetlistViewer/override-third-party.json -t -o Table -d ./publish/THIRD-PARTY-LICENSES -fo ./publish/THIRD-PARTY-NOTICE.txt
      - name: Copy font license for viewer
        id: font-license
        run: cp ./src/FEntwumS.NetlistViewer/Assets/Fonts/MartianMono-LICENSE.txt ./publish/THIRD-PARTY-LICENSES/Martian-Mono-License.txt
      - name: Build interactor
        run: dotnet build src/FEntwumS.WaveformInteractor/FEntwumS.WaveformInteractor.csproj -c Release -o publish/FEntwumS.WaveformInteractor
      - name: Generate licensing information for interactor
        run: nuget-license -i ./src/FEntwumS.WaveformInteractor/FEntwumS.WaveformInteractor.csproj -override ./src/FEntwumS.WaveformInteractor/override-third-party.json -t -ot Table -d ./publish/FEntwumS.WaveformInteractor/THIRD-PARTY-LICENSES -fo ./publish/FEntwumS.WaveformInteractor/THIRD-PARTY-NOTICE.txt
      - name: Compress
        if: steps.license.outcome == 'success'
        uses: thedoctor0/zip-release@0.7.1
        with:
          type: 'zip'
          filename: ../FEntwumS.NetlistViewer_${{ github.ref_name }}_all.zip
          directory: ./publish
      - name: Debug
        run: ls && ls -lshaR publish
      - uses: ncipollo/release-action@v1.14.0
        with:
          artifacts: "FEntwumS.NetlistViewer_${{ github.ref_name }}_all.zip,./publish/compatibility.txt"
          allowUpdates: true
          replacesArtifacts: false
          generateReleaseNotes: true
